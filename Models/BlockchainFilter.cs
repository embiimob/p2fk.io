namespace P2FK.IO.Models
{
    /// <summary>
    /// Supported blockchain filter values for the search endpoints.
    /// Leave unset to return results from all blockchains.
    /// </summary>
    public enum BlockchainFilter
    {
        BTC,
        LTC,
        DOG,
        MZC
    }
}
